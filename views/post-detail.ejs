<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: post.title }) %>
    <link rel="stylesheet" href="/styles/posts.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <main id="post-detail">
      <h1><%= post.title %></h1>
      <section id="post-meta">
        <!-- address tag used when we have contact detail as data-->
        <address>
          <a href="mailto:<%= post.author_email %> "
            ><%= post.author_name %>
          </a>
        </address>
        |<!-- time tag used to show timestamp in webpage if used as input, its attribute takes machine readable time but output the human readable time -->
        <time datetime="<%= post.date %>"><%= post.humanReadableDate %> </time>
      </section>
      <hr />
      <section>
        <p id="body"><%= post.body %></p>
      </section>
    </main>
  </body>
</html>
